// "Clinical stationery": a warm paper background, deep ink text, one muted
// accent used sparingly. No glow, no gradients, no glass, no drop shadows —
// structure comes from thin rule lines, not cards. Shared by every onboarding
// screen so the funnel reads as one design instead of one-off palettes.
export const PAPER = "#EEF1EA";
export const INK = "#16302B";
export const INK_MUTED = "#4B5C50";
export const INK_FAINT = "#6B7A70";
export const ACCENT = "#C9A227";
export const RULE = "#C7CFC2";
export const RULE_STRONG = "#6B8F82";

// Semantic colors for meaning that can't be conveyed by ink alone (urgency,
// status) — desaturated to stay in the same restrained family rather than
// reaching for saturated red/green/yellow.
export const SUCCESS = "#3F7A5C";
export const WARNING = ACCENT;
export const DANGER = "#9C3B2E";
